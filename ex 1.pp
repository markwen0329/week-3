#include <stdio.h>

int main() {
    int a, b, product;
    a = 20;
    b = 7;

 
    // Calculating product
    product = a * b;
    a = a + 3;
 
    printf("Product = %d\n", product);
    printf("a = %d\n", a);
    return 0;
}
